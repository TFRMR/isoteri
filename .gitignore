# Rust build output
target/

# Editor / OS files
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs / temporary files
*.log
*.tmp
*.swp
*~

# Local environment / secrets
.env
.env.*
!.env.example

# Generated browser/demo output
# Keep source .iso and checked-in .isoweb.json files; ignore only ad-hoc output.
runtime/web/dist/
runtime/web/build/

# wasm-pack build output (regenerate with: wasm-pack build --target web --out-dir pkg)
isoteri-wasm/pkg/
runtime/web/pkg/

# Binary hasil `isoteri bangun` (AOT) -- regenerate via perintah di
# benchmarks/head_to_head/README.md, jangan commit binary besar ke git.
benchmarks/head_to_head/isoteri/*_aot
benchmarks/representasi/*_aot
